fastapi>=0.115
uvicorn[standard]>=0.32
pydantic>=2.9
pydantic-settings>=2.6
sqlalchemy>=2.0
alembic>=1.13
apscheduler>=3.10
anthropic>=0.100
httpx>=0.27
python-dotenv>=1.0
# Postgres driver — installed but only used when DATABASE_URL points at Postgres.
psycopg[binary]>=3.2
# Google Calendar direct OAuth + API (Phase 4).
google-auth>=2.30
google-auth-oauthlib>=1.2
google-api-python-client>=2.140
# Test deps (kept in main requirements for now; split into requirements-dev.txt later if needed).
pytest>=8.0
