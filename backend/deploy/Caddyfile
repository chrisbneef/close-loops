# Cadence brain — reverse proxy + automatic HTTPS.
# api.closedloops.tech must already point at this VPS via a DNS A record. Caddy
# provisions + renews the Let's Encrypt cert automatically the first time it
# starts with the domain resolving here.

api.closedloops.tech {
    reverse_proxy 127.0.0.1:8000
}
